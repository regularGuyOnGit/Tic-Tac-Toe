// sepration of the logic is main concern here.

const gameboard = ()=>{

const column = 3;
const row = 3;
const gameBoard = [];

for(let i=0;i<row;i++){
gameBoard[i] = [];

for(let j =0 ; j<column;j++){
    // Need to push the column inside of the 2d array so it can mimic the tic tac toe layout.
    gameBoard[i].push();
}
}
}



const player ={
    // ! an obect to store player
}
const gameFlow = {
    // ! game flow should be here to perform the game logic.
}